// Tags handling
// =================================================

package main

import (
//    "gopkg.in/yaml.v2"
)

func populateTagsMap() {
    if ! fileExists(directory + "/tags.yaml") {
        return
    }
}
